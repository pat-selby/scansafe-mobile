# Product Roadmap — ScanSafe

> Generated by the Product Planner skill. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 28/51 tasks complete
**Current Phase:** Phase 2 — Camera & OpenCV Decode (unblocked — Android builds and runs on the emulator)

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** The first phase sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The core value proposition works as early as possible. Everything else builds on top.
4. **Test as you go.** Don't save testing for the end. Each task includes verification.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent review it before starting the next phase.
7. **Parity is the acceptance criterion.** The engine must score identically to the Python research prototype. A red parity test means the engine drifted — fix the engine, never the corpus.
8. **`lib/core/` stays pure.** No Flutter imports, no I/O, no network. That constraint is what makes the privacy claim structural rather than aspirational.

---

## Phase 0: Foundation & the Rule Engine

> **Goal:** The 22-rule engine runs in pure Dart and provably matches the Python prototype, inside a themed app shell that launches.

**Reference sections — read these before starting this phase:**
- PRD: § Technical Architecture, § Data Model, § API Specification
- Design: `docs/design.md` (token YAML front matter + § Colors, § Typography, § Layout)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/design.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation`, commit, push, and open a PR for review."

- [x] **TASK-001** — Scaffold the Flutter project targeting android and ios, org `edu.grambling.aiot`, project name `scansafe`
  Files: `pubspec.yaml`, `lib/main.dart`, `android/`, `ios/`
  Notes: `flutter create --platforms=android,ios --org edu.grambling.aiot --project-name scansafe .` Set the display name to "ScanSafe" in both platform manifests.

- [x] **TASK-002** — Add and pin dependencies
  Files: `pubspec.yaml`
  Notes: `shared_preferences` only for now. Camera, permission_handler, OpenCV and Sentry arrive in their own phases so a broken native dependency can never block engine work. Do NOT add `url_launcher` — its absence is a deliberate safety control (PRD § Security Considerations).

- [x] **TASK-003** — Implement the design tokens as a Flutter theme
  Files: `lib/ui/theme.dart`
  Notes: Transcribe the YAML block in `docs/design.md` exactly — colors, typography, rounded, spacing. Expose a `verdictColors(RiskLevel)` helper returning foreground/surface/border. Never inline a hex value anywhere else in the app.

- [x] **TASK-004** — Build the data model with a JSON round-trip
  Files: `lib/core/risk_level.dart`, `lib/core/finding.dart`, `lib/core/scan_result.dart`
  Notes: `RiskLevel.fromScore` implements 0-2 / 3-5 / 6+. `Finding` carries ruleId, plain, technical, points. Rule id `0` is the sentinel for non-rule notices. No Flutter imports in any of these files.

- [x] **TASK-005** — Port the rule constants verbatim from the Python prototype
  Files: `lib/core/rule_data.dart`
  Notes: Source is `scan-safe/scansafe/scansafe_prototype.py`. Brand list, free hosting platforms, suspicious TLDs, risky ccTLDs, shorteners, redirect wrappers, allowlist, bad keywords, urgency keywords, dangerous schemes, known SLDs, homoglyph map. Copy values exactly — do not "improve" them; the published evaluation used these.

- [x] **TASK-006** — Port the fuzzy-matching layer
  Files: `lib/core/fuzzy.dart`
  Notes: LCS length + similarity, `apexDomain` (two-part TLD aware, `replaceAll` on `www.` not a prefix strip), homoglyph normalisation, DJB2 token hash, 64-bit SimHash, Hamming distance, and an injectable `SimHashCache`. Use `BigInt` for the hash — Dart's `int` is 32-bit for bitwise ops when compiled to JavaScript and would corrupt the fingerprint.

- [x] **TASK-007** — Reimplement Python's `urlparse` splitting
  Files: `lib/core/parsed_url.dart`
  Notes: Dart's `Uri` lowercases the host, strips the port from the authority, and re-encodes paths — each silently changes a rule outcome. Split in Python's order: fragment, scheme, netloc, query, path. `netloc` must include the port, because Rules 2, 5, 10 and 15 depend on it.

- [x] **TASK-008** — Implement Rules 1-18 in the scorer
  Files: `lib/core/url_scorer.dart`
  Notes: Lowercase the whole URL first, unwrap `blob:`, prepend `https://` only for bare domains. Allowlist bypass returns early with a sentinel finding. Rules are additive and independent.

- [x] **TASK-009** — Implement Rules 19-22 plus Rule 13's recursion
  Files: `lib/core/url_scorer.dart`
  Notes: Rule 19 (apex LCS), 19b (path-segment homoglyph LCS at threshold 0.80, reported as rule 19), 20 (SimHash near-duplicate), 21 (urgency), 22 (free hosting). Rule 13 recursively scores the extracted inner URL and folds in its score — cap recursion depth at 3 so a self-referencing wrapper cannot hang the scorer.

- [x] **TASK-010** — Generate the parity corpus and make it pass
  Files: `tool/generate_parity_corpus.py`, `tool/generate_parity_test.py`, `tool/parity_corpus.json`, `test/url_scorer_test.dart`
  Notes: Import the Python prototype, clear its global `_SIMHASH_CACHE` before each URL, score a corpus covering every rule, emit JSON, generate the Dart test from it. Compare score, level, and fired-rule set (excluding sentinel rule 0). Run the prototype from the `scan-safe` venv, which has OpenCV installed.

- [x] **TASK-011** — Add unit tests for the fuzzy primitives and thresholds
  Files: `test/url_scorer_test.dart`
  Notes: LCS identity and empty-string cases, apex extraction for two-part TLDs, SimHash stability and separation, Rule 20 not firing on first sighting and not leaking between scorer instances, verdict threshold boundaries at 2/3 and 5/6.

- [x] **TASK-012** — Wire the themed app shell and verify the phase
  Files: `lib/main.dart`
  Notes: `MaterialApp` using the theme from TASK-003, dark-only, no debug banner, placeholder home. Verify: `flutter analyze` reports no issues, `flutter test` all green, `flutter run -d chrome` launches without error.

---

## Phase 1: Verdict, Findings & History

> **Goal:** Someone can paste a link, get a verdict they understand with reasoning they can inspect, and find that scan again later.

**Reference sections — read these before starting this phase:**
- PRD: § Data Model, § User Stories, § Functional Requirements, § UI/UX Requirements, § Edge Cases & Error Handling
- Vision: § Voice & Tone Guide, § Product Principles
- Design: `docs/design.md` § Components, § Colors, § Do's and Don'ts

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/design.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/verdict-and-history`, commit, push, and open a PR for review."

- [x] **TASK-013** — Build the verdict badge widget
  Files: `lib/ui/widgets/verdict_badge.dart`
  Notes: Icon + verdict word + score, using the `verdict-safe` / `verdict-suspicious` / `verdict-high-risk` component tokens. One widget parameterised by level, not three implementations. A `compact` pill variant at `rounded.pill` for history rows. Colour is never the only signal.

- [x] **TASK-014** — Build the dual-layer finding tile
  Files: `lib/ui/widgets/finding_tile.dart`
  Notes: `finding-row` token. Plain-English text always visible, `+N` weight in `on-surface-variant`, "See details" text button revealing a `finding-detail` monospace panel. Technical text is selectable.

- [x] **TASK-015** — Build the result screen
  Files: `lib/ui/screens/result_screen.dart`
  Notes: Verdict badge, headline sentence from the voice guide, URL in a monospace `url-display`, then findings or the clean-result explanation. Closing advice card for non-safe verdicts. The URL is selectable but NEVER tappable — no link affordance anywhere (FR-13).

- [x] **TASK-016** — Build the scan history store
  Files: `lib/data/scan_history_store.dart`
  Notes: `shared_preferences`, JSON list, newest first, 50-item cap dropping oldest on overflow. A corrupt blob is discarded and returns empty rather than throwing (PRD § Edge Cases).

- [x] **TASK-017** — Build the history screen
  Files: `lib/ui/screens/history_screen.dart`
  Notes: Monospace URL plus compact verdict pill per row. Swipe-to-delete, clear-all behind a confirmation dialog, and an empty state stating history is local-only and never uploaded.

- [x] **TASK-018** — Build the home screen with manual URL entry
  Files: `lib/ui/screens/home_screen.dart`
  Notes: Headline, privacy line, `input-url` field, `button-primary` "Scan this link", `button-scan` dashed QR button (disabled until Phase 2, labelled so), history icon in the app bar. Hold one `UrlScorer` for the app session so Rule 20 can see earlier scans.

- [x] **TASK-019** — Wire navigation and persist every scan
  Files: `lib/main.dart`, `lib/ui/screens/home_screen.dart`
  Notes: Home → Result, Home → History, History → Result. Every scored URL is written to history before navigating.

- [x] **TASK-020** — Implement empty, invalid, and error states
  Files: `lib/ui/screens/home_screen.dart`, `lib/ui/screens/result_screen.dart`
  Notes: Empty input shows inline validation without navigating. An unparseable URL scores 10 / HIGH RISK with an explanatory finding. Copy comes from the § Voice & Tone Guide table — never a raw error code.

- [x] **TASK-021** — Accessibility pass
  Files: `lib/ui/**`
  Notes: Semantic labels on every control, 44px minimum touch targets, verdict meaning never carried by colour alone, layout intact at 200% text scale. Verify with the accessibility inspector.

- [x] **TASK-022** — Widget tests for the scan, result, and history flows
  Files: `test/widget_test.dart`
  Notes: Risky link produces HIGH RISK with findings; empty input does not navigate; technical detail hidden until tapped; history caps at 50 and round-trips findings through storage; corrupt blob survives.

- [x] **TASK-023** — Add haptic feedback on verdict
  Files: `lib/ui/screens/result_screen.dart`
  Notes: Light impact for SAFE, heavier for SUSPICIOUS and HIGH RISK. Never a continuous or alarming pattern — see § Do's and Don'ts.

- [x] **TASK-024** — Verify the phase end to end
  Files: —
  Notes: Run the app, paste `https://ivoryrobinson94.wixsite.com/0ne-dr1ve`, confirm HIGH RISK with Rules 19 and 22 explained in plain English, confirm it appears in history, delete it, clear all. `flutter analyze` clean, all tests green.

---

## Phase 2: Camera & OpenCV Decode

> **Goal:** Pointing the phone at a real QR code produces a verdict before any page could load. This phase delivers the magic moment.

**Reference sections — read these before starting this phase:**
- PRD: § Technical Architecture, § Stack Integration Guide, § Functional Requirements, § Edge Cases & Error Handling, § Dependencies & Integrations
- Design: `docs/design.md` § Components (`scan-reticle`, `button-scan`)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above from docs/prd.md and docs/design.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/camera-decode`, commit, push, and open a PR for review."

- [x] **TASK-025** — Fix the Android toolchain so device builds work
  Files: —
  Notes: Resolved 2026-09-07 — and the premise was wrong. `flutter doctor` reports missing
  `cmdline-tools`, which looked like a build blocker but is not one: `cmdline-tools` only
  provides `sdkmanager`/`avdmanager` for *installing* SDK components and accepting licences.
  The licence (`android-sdk-license`) was already accepted, and platforms android-34/36 plus
  build-tools 34.0.0/36.1.0 were already present, so Gradle had everything it needed.
  `flutter build apk --debug` succeeds (684s on the first run, which downloads Gradle and
  CMake). The debug APK installs and runs on the `Medium_Phone_API_36.1` emulator, confirmed
  via `dumpsys window` (`mCurrentFocus` = MainActivity) with no crashes in logcat.
  Note: `adb exec-out screencap` returns a stale framebuffer on this emulator — verify the UI
  by looking at the emulator window, not by screenshotting over adb.
  Install `cmdline-tools` anyway when convenient, so `flutter doctor` is clean and
  `sdkmanager` is available — but it does not gate any task.

- [x] **TASK-026** — Choose and pin the OpenCV package, and record the decision
  Files: `pubspec.yaml`, `docs/prd.md` § Open Questions
  Notes: Must expose `cv::QRCodeDetector` on both Android and iOS. Evaluate build size and native binary handling before committing. Do NOT substitute `mobile_scanner`, ML Kit, or ZXing — OpenCV is a research constraint. Update PRD Open Question 1 with the answer.

- [ ] **TASK-027** — Define the decoder interface and a stub implementation
  Files: `lib/core/qr_decoder.dart`
  Notes: `Future<String?> decode(List<int> imageBytes)`. A `StubQrDecoder` returning a scripted payload lets the scan screen be built and tested before the native binding works.

- [ ] **TASK-028** — Implement `OpenCvQrDecoder` with the four-stage fallback
  Files: `lib/core/opencv_qr_decoder.dart`
  Notes: In order: original colour frame, BT.601 greyscale, Otsu binarisation for coloured/branded codes, inverted Otsu for light-on-dark. Stop at the first stage returning a payload. Single-stage decoding failed on real samples — all four stages are required.

- [ ] **TASK-029** — Test the decoder against the research repo's QR fixtures
  Files: `test/qr_decoder_test.dart`
  Notes: `scan-safe/scansafe/assets/images/` holds safe, suspicious, high-risk, blob, and 2FA sample codes. Each must decode, and its scored verdict must match what the filename implies.

- [ ] **TASK-030** — Add camera permission handling with distinct states
  Files: `lib/ui/screens/scan_screen.dart`, `pubspec.yaml`
  Notes: `permission_handler`. Granted, denied, and permanently-denied are three different states with different copy. Denied must offer a settings deep link and keep manual entry working — the app stays useful without the camera (FR-12).

- [ ] **TASK-031** — Build the camera preview screen
  Files: `lib/ui/screens/scan_screen.dart`
  Notes: Full-bleed preview, centred reticle at the `scan-reticle` token size, close control, guidance copy. Add the iOS `NSCameraUsageDescription` and the Android camera permission with strings explaining the on-device-only purpose.

- [ ] **TASK-032** — Stream frames with throttling and stop on first decode
  Files: `lib/ui/screens/scan_screen.dart`
  Notes: Throttle decode attempts to 4-6/second to protect battery. Stop the stream the instant a payload decodes. Frames must never be written to disk (PRD § Security Considerations).

- [ ] **TASK-033** — Wire decode → score → result and enable the QR button
  Files: `lib/ui/screens/home_screen.dart`, `lib/ui/screens/scan_screen.dart`
  Notes: Decoded payload goes straight into the same `UrlScorer` the manual path uses, then to the result screen, then to history. Enable the dashed scan button and drop its "coming next" label.

- [ ] **TASK-034** — Handle non-URL payloads
  Files: `lib/ui/screens/result_screen.dart`
  Notes: A Wi-Fi join string or vCard decodes but is not a URL. Show the decoded text clearly marked "not a link — not scored" rather than scoring it as a bare domain (FR-15).

- [ ] **TASK-035** — Add decode-timeout guidance
  Files: `lib/ui/screens/scan_screen.dart`
  Notes: After roughly 8 seconds without a decode, show "The camera couldn't read that code. Try moving a little closer." Keep scanning — never dismiss the screen on the user's behalf.

- [ ] **TASK-036** — Add a torch toggle where the device supports one
  Files: `lib/ui/screens/scan_screen.dart`
  Notes: Hide the control entirely when the device reports no torch, rather than showing a disabled one (FR-14).

- [ ] **TASK-037** — Verify the magic moment on real hardware
  Files: —
  Notes: Print a QR code encoding `https://ivoryrobinson94.wixsite.com/0ne-dr1ve`. On a physical Android device, point and confirm HIGH RISK appears before any page could load, with the impersonation named in plain English. Measure decode-to-verdict latency against the 500ms target.

---

## Phase 3: Hardening & Store Release

> **Goal:** ScanSafe is measured, accessible, privacy-audited, and submitted to both stores.

**Reference sections — read these before starting this phase:**
- PRD: § Non-Functional Requirements, § Edge Cases & Error Handling, § Security Considerations, § Open Questions
- Vision: § Success Metrics, § Risks
- Design: `docs/design.md` § Do's and Don'ts

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/design.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/release`, commit, push, and open a PR for review."

- [x] **TASK-038** — Resolve the `grambling.edu` false positive
  Files: `lib/core/rule_data.dart`, `lib/core/url_scorer.dart`, `test/url_scorer_test.dart`
  Notes: Currently scores 4 (SUSPICIOUS) — Rule 8 fires because `gram` is a substring of `gramblingedu`, Rule 15 because `grambling` is 78% consonants. **Release blocker.** Likely fix: drop `gram` from the brand list and require a minimum brand length. Regenerate the parity corpus deliberately and document the divergence from the prototype in the PRD.

- [x] **TASK-039** — Build a legitimate-domain corpus and measure the false-positive rate
  Files: `tool/measure_false_positives.py`, `docs/evaluation.md`
  Notes: At least 200 real domains students actually visit — university, banking, retail, streaming, campus services. Target under 5%. Publish the number; it is a success metric, not a nice-to-have.

- [ ] **TASK-040** — Measure decode-to-verdict latency
  Files: `docs/evaluation.md`
  Notes: Instrument the scan path on mid-range Android hardware. Target under 500ms. Report scoring time separately — it should be under 10ms.

- [ ] **TASK-041** — Integrate Sentry with URL scrubbing
  Files: `lib/main.dart`, `pubspec.yaml`
  Notes: `beforeSend` must strip any breadcrumb, extra, or exception message that could contain a scanned URL, payload, or history entry. Write a test proving a URL cannot reach an outgoing event. DSN via `--dart-define`, never committed.

- [ ] **TASK-042** — Add the rule reference screen
  Files: `lib/ui/screens/rules_screen.dart`
  Notes: All 22 rules with weights and one-line explanations. This is the explainability promise made browsable, and it is what security-awareness staff will project during training (FR-17).

- [ ] **TASK-043** — Write the privacy policy
  Files: `docs/privacy-policy.md`
  Notes: Must state plainly: scans never leave the device, history is local-only, no accounts, no analytics, camera used only for decoding, frames never stored. Disclose Sentry crash reporting as the single network call and state that scan content is never attached. Both stores require a hosted URL.

- [ ] **TASK-044** — Create app icons and splash screens
  Files: `android/app/src/main/res/`, `ios/Runner/Assets.xcassets/`
  Notes: Derive from the design tokens — accent green on true black. Generate every required density for both platforms.

- [ ] **TASK-045** — Full accessibility audit
  Files: `lib/ui/**`
  Notes: Screen reader pass on every flow including the camera screen, 200% text scale on every screen, contrast verification against WCAG AA, and confirmation that no verdict depends on colour alone.

- [ ] **TASK-046** — Configure the Android release build and signing
  Files: `android/app/build.gradle`, `android/key.properties`
  Notes: Release keystore, ProGuard rules for the OpenCV native binaries, build an AAB. Keystore and passwords never committed.

- [ ] **TASK-047** — Configure the iOS release build
  Files: `ios/Runner.xcodeproj`, `ios/Runner/Info.plist`
  Notes: Requires a Mac with Xcode — the primary dev machine is Windows. Signing certificate, provisioning profile, camera usage description, archive and validate.

- [ ] **TASK-048** — Prepare store listings
  Files: `docs/store-listing.md`
  Notes: Title, subtitle, description, keywords, and screenshots at every required size for both stores. Lead with the privacy position — it is the strongest differentiator. Copy comes from § Elevator Pitches and § Messaging Framework.

- [ ] **TASK-049** — Run the BuilderOS Launch Checklist skill
  Files: —
  Notes: Full pre-submission audit. Resolve everything it flags before submitting.

- [ ] **TASK-050** — Submit to both stores
  Files: —
  Notes: Google Play internal testing track first, then production. App Store Connect via TestFlight, then review. Expect rejections on privacy-policy wording and camera-usage justification — both are answerable directly from `docs/privacy-policy.md`.

- [ ] **TASK-051** — Decide how Rule 8 should treat brands on legitimately-owned parent domains
      Files: `lib/core/url_scorer.dart`, `docs/evaluation.md`
      Notes: Five legitimate URLs still score SUSPICIOUS — `outlook.office.com`,
      `login.microsoftonline.com`, `onedrive.live.com`, `gsumail.grambling.edu`. Rule 8
      flags a brand appearing on an apex that is not the brand, which is exactly what
      large organisations legitimately do. Options are a brand-to-parent-domain map
      (allowlisting — already flagged as a methodological weakness in the research) or a
      better ownership signal. This is a research decision; measure any change with
      `tool/measure_false_positives.py` **and** `tool/measure_detection.py` before adopting
      it, and record the outcome in `docs/evaluation.md`.
